# World — a Core-level fragment carrying the map-seeding knobs (a runtime extension on Core).
# sector-scale multiplies authored per-sector radii; asteroid-density scales asteroid counts.
# Reproduces stock.yaml's world block.
world:
  id: 0
  sector-scale: 2.25
  asteroid-density: 1.0
  debug-freeze-brain: false
  debug-no-fire: false
