<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Link the client's pure-POCO crew store directly. CrewStore depends only on the BCL — NOT on
         Godot and not on any client seam — so this suite exercises the real production reconcile /
         version / lookup logic headlessly. Same idiom as tests/TeamStateStoreTest. -->
    <Compile Include="../../client/scripts/world/CrewStore.cs" Link="CrewStore.cs" />
  </ItemGroup>
</Project>
