# 99 — Open Questions & Decision Log

Append here whenever a decision exceeds the spec, an API turns out different from what these
docs assumed, or a deferred choice needs to be made. Format: date, context, decision/assumption,
and whether a human should review.

## Pre-seeded items the agent will likely hit

1. **SpacetimeDB 2.0 scheduled-reducer mechanism.** `04` assumes `SimTick` is driven by a
   scheduled reducer at 20 Hz. Confirm the exact 2.0 scheduling API (scheduled table + reducer
   wiring) from the functions reference and record the concrete pattern used here.

2. **Shared math types.** `06` flags that Godot's `Vector3`/`Quaternion` may not match the
   module's math types. Decision needed: use a self-contained math struct in `shared/` (most
   robust) vs. per-side adapters. Record which was chosen and why.

3. **`ShipInput` table access control.** `03` keeps it public for prototype simplicity.
   Decide post-prototype whether to restrict writes to the owning identity.

4. **Asteroid collision response.** `04` step 6 leaves ship-vs-asteroid collision optional for
   the first pass. Record whether it was included, deferred, or stubbed.

5. **Input send when unchanged.** `07` allows sending `ApplyInput` at 20 Hz even when input is
   unchanged. If reducer-call volume becomes a free-tier concern, switch to send-on-change +
   periodic keepalive and record the change.

6. **Client SDK package name/version.** `02` references adding the SpacetimeDB C# client SDK
   via NuGet. Record the exact package and version actually used, since this moves between
   releases.

7. **Maincloud free-tier limits.** Confirm current free-tier reducer-call / connection limits
   before the T10 two-machine test, and note headroom for the prototype's call volume.

## Decision log

| Date | Context | Decision / Assumption | Needs human review? |
|------|---------|------------------------|---------------------|
| 2026-06-01 | #2 shared math types (T3) | Used a **self-contained `Vec3`/`Quat`** in `shared/FlightModel.cs` (not Godot or System.Numerics types) so the module and client copies are byte-identical and engine-independent. All in namespace `StellarAllegiance.Shared`; `StatsFor(byte)` so the file depends on no game enum. Canonical copy is `shared/FlightModel.cs`; `shared/sync.sh` copies it verbatim into `module/spacetimedb/` and `client/scripts/`. | No |
| 2026-06-01 | Transcendental determinism (T3) → **CONFIRMED at T5** | `Integrate` uses `Math.Sin/Cos/Sqrt`. Same source both sides, but wasm (server) and mono (client) compute them slightly differently. **Confirmed at T5:** after tick alignment removed all systematic error, the residual divergence is exactly this — ~0.0125 u/tick, caught by reconciliation (~1 u correction ~0.25 Hz at 1.0 u tolerance). Not a blocker; reconciliation handles it. True fix (if ever needed): a shared fixed-point/polynomial `sin/cos`. | Acceptable; revisit only if WAN play (T10) shows it |
| 2026-06-01 | Angular drag stat (T3) | `.PLAN/03` stats table omits angular drag. Chose `AngularDrag` = 2.5 (Scout) / 2.0 (Fighter) as tunable placeholders; tune in T4. | No |
| 2026-06-01 | Local DB ownership | A stray `spacetime sql` auto-`login` overwrote `.stdb-config` with a non-owner identity → publish 403. Fixed by restarting the (ephemeral) server and republishing fresh. Avoid running CLI subcommands that trigger a fresh login against the mounted config. | No |
| 2026-06-01 | Ship angular velocity (T4) | The flight model integrates+damps angular velocity, but `.PLAN/03`'s `Ship` table had no angular field. **Added `AngVelX/Y/Z` floats to `Ship`** so rotational momentum persists between SimTicks and the client can reconcile against it. Schema change → republished `--delete-data`, regenerated bindings. | No |
| 2026-06-01 | SpawnShip phase guard (T4) | `.PLAN/04` says reject spawn unless `Phase == Active`, but Active needs both teams populated — impossible to fly solo for the T4 single-player gate. **Relaxed to allow Lobby or Active; only `Ended` blocks spawning.** Revisit if the two-team flow needs stricter gating. | Maybe |
| 2026-06-01 | #4 asteroid collision (T4) | **Deferred** — ships currently pass through asteroids. Build order defers collision response; revisit in/after T8. | No |
| 2026-06-01 | Prediction/server tick aliasing (T4) | Client advances one predicted step per `clientTick`; server advances one per `SimTick`. On localhost with near-zero latency, occasional (2–5 over a few seconds) reconciliations still fire from phase/step aliasing — not rubber-banding. `PredictionController.ReconcileCount` instruments this; harden + measure properly at T5. | Superseded below |
| 2026-06-01 | Chase-cam jerk — investigation + SIMPLIFIED fix (T4) | A ~1 Hz chase-cam jerk was chased through several wrong turns; recording the conclusion so it isn't re-attempted. **What it was NOT:** not a slow server (measured ~19–20 Hz), not the tick-label mismatch alone. **Two real causes:** (1) `PredictionController` originally **extrapolated** the visual (race/stall/jump on the tick accumulator) — fixed by prev→current **interpolation**; (2) the actual jerk in the build the user tested was a **`Quaternion is not normalized` exception thrown every frame** from over-engineered rotation-correction smoothing (`_rotSmooth.Slerp` drifted off unit length), which aborted the ship's per-frame transform update so the camera read a stale transform. **Reverted/abandoned approaches** (added complexity, didn't help, one made it worse): server stamping `Match.Tick`, client prediction-tick slaved to server with `MinLead`/`MaxLead` bounding (its "pause when too far ahead" introduced its own ~1 Hz stall), and the rotation-correction smoothing. **Final simple state:** server stamps `ClientTick` (original); client runs a plain fixed-20 Hz prediction loop; `PredictionController` renders interpolated prediction with quaternions normalized before `Slerp`; only simple position easing on the rare reconcile; `CameraRig` rigidly locks to the ship transform (moves at exactly the ship's rate). Reconcile tolerance widened to 8 u so the **bounded, legitimate prediction lead** (client is correctly a few ticks ahead of the timer-driven, input-coalescing server) is not mistaken for an error. Measured: 0 quaternion exceptions; ~0.23 Hz reconciles over ~116 s (down from ~1.5 Hz), position-smoothed. **PARTIALLY SUPERSEDED at T5** — see next row: the "don't re-add tick stamping / slaved clock" conclusion was wrong; those were re-introduced *correctly* at T5 (the thing to never re-add is the rotation-correction smoothing and the lead-bounding *stall*). | No |
| 2026-06-01 | Spawn inside base mesh (T4) | Ships spawn at the exact base center, inside the 45-unit base sphere (back-face culling makes it see-through from within, so the view is fine). Minor visual polish; could add a small launch offset later. | No |
| 2026-06-01 | `--autofly` dev flag (T4) | `ShipController` honors a `--autofly` command-line arg that auto-spawns a Scout and flies a fixed input — used for headless verification of the ApplyInput→SimTick→reconcile loop. Harmless dev affordance, left in place. | No |
| 2026-06-01 | Tuned flight constants (T4) | Using `.PLAN/03` placeholders: Scout thrust 45 / maxspeed 70 / lindrag 1.2 → terminal ≈35 u/s (observed). Mechanically responsive via prediction. **Subjective "is it fun" gate still needs a human at the keyboard** (WASD thrust/strafe, Space/Shift up-down, arrows aim, Q/E roll). | Yes (playtest) |
| 2026-06-01 | **Reconciliation — tick alignment (T5, the correct fix)** | Root cause of the T4 8.8 u steady divergence: server runs at **~18.7 Hz** (measured over 8 s; the 50 ms scheduled reducer doesn't hit 20 Hz), so a 20 Hz client and the server drift in *integration count*; with `ClientTick` stamping, `predicted[N]` ≠ `auth[N]` by a growing-then-reconcile-capped amount, even in straight flight (`lead=0` proved it wasn't latency lead). **Fix:** server stamps `LastInputTick = Match.Tick` (integration index); client predicts in that tick space, anchoring `_predTick` to `ServerTick` on spawn and advancing on a **slewed** local clock (rate nudged ±`MaxSlew`, gain `SlewGain`, to hold `TargetLead=2`). No lead-bounding *stall* (that was the T4 mistake), no rotation smoothing (the exception source). Result: lead bounded **2–3 ticks**, steady error **~0.5 u** (was 8.8), tolerance tightened to **1.0 u**. Injected 25 u divergence recovers in **one** correction. Residual ~1 u/0.25 Hz reconciles = the transcendental float drift (row above). This is the genuinely correct netcode; the earlier "don't re-add tick stamping" note was based on a buggy *implementation*, not the approach. | No |
| 2026-06-01 | **Deterministic trig — turn jerk (T5)** | After tick alignment, turning still jerked while straight flight was smooth. Cause: wasm-server vs mono-client `Math.Sin/Cos` (libm) differ in the last bits; integrated every tick this drifts the *rotation* (`rotErr` ~0.05–0.07), and the chase cam amplifies each reconcile. IEEE `+,-,*,/,sqrt` ARE bit-identical across the two runtimes; only transcendentals aren't. **Fix:** `MathDet.Sin/Cos` in `shared/FlightModel.cs` — a Taylor polynomial in plain float ops (Horner, no FMA), range-reduced with truncating casts. Both runtimes now compute bit-identical rotations (`rotErr→0` under constant input). Accuracy vs true sin/cos is irrelevant; *agreement* is the point. Updated the golden test. `sqrt` left as `Math.Sqrt` (correctly-rounded → already deterministic). | No |
| 2026-06-01 | **Per-tick input buffer — zero divergence (T5 final)** | After deterministic trig, the last residual was the latest-input server model: while INPUT changes (steering), the server applied the newest input at tick N while the client predicted N with the input it computed a tick earlier → bounded transient ∝ lead (tolerance only capped it). **Fix:** `ShipInput` changed from one-row-per-ship (PK `ShipId`, overwritten) to a **per-tick buffer** (PK `InputId` autoinc, btree index on `ShipId`, field `Tick`); `ApplyInput` upserts `(ship, tick)`; `SimTick` applies the input stamped for the current tick (fallback: most recent `Tick ≤ N`); pruned to 64 ticks; made **server-private** (clients write via reducer, never read it). With tick alignment the client's `_predTick` and server `Match.Tick` match, so the server replays the client's exact input sequence → `predicted[N] == auth[N]` bit-for-bit. Measured: **0 drift reconciles** over ~5 min continuous weave at tight 1.0 u/0.05 rad tolerance (only a one-time spawn-anchor + injection). Schema change → `--delete-data` + regen. `TargetLead` reduced to **1** (localhost). | No |
| 2026-06-01 | Remote interpolation (T6) | `RemoteShip` renders other players' ships via snapshot interpolation: buffer authoritative transforms (arrival-timestamped), render at `now − 100 ms` (`InterpDelayMs`), lerp pos / slerp rot between bracketing samples, hold-latest if no newer sample (no extrapolation, per `.PLAN/07`). Verified two headless clients render each other's ship interpolating with zero errors. | No |
| 2026-06-01 | Control remap (T6) | Switched to Allegiance-style 6DOF: `W/S` throttle, `A/D` strafe L/R, `E/C` strafe up/down, `Q/Z` roll, arrows yaw/pitch (was WASD-strafe + Space/Shift up-down + Q/E roll). Roll moved off `E` since `E` is now strafe-up. Yaw/pitch still on arrows — mouse-look (true Allegiance aim) is a possible future enhancement, not requested yet. | No |
| 2026-06-01 | Both ship classes + spawn menu (T7) | `SpawnShip(ShipClass)` and `FlightModel.Fighter` stats already existed, so T7 was client-only: (1) `Hud` now shows a **spawn menu** (Scout/Fighter `Button`s in a `VBoxContainer`, visible only when shipless) that calls `ShipController.RequestSpawn(class)`; the `1`/`2` keys are kept as shortcuts. (2) `WorldRenderer.BuildShipMesh(team, class)` gives the Fighter a **chunky `BoxMesh`** vs the Scout's sleek cone. (3) Spawn flow refactored to a nullable `_spawnRequest` class (menu/keys/autofly all set it; reducer fires once connected, with retry). Dev flag **`--fighter`** makes `--autofly` spawn a Fighter for headless verify. **Verified headless:** Scout (#4) and Fighter (#3) both spawn with the right `Class` and integrate — Scout cruises ~34 u/s vs Fighter ~26 u/s, matching the higher Scout thrust/maxspeed. **Subjective "feel distinct" gate still needs a human** (like T4's fun gate). | Yes (playtest) |
| 2026-06-01 | Weapons, damage, death, collisions (T8) | **Server (`SimTick`, restructured into 3 passes):** Pass A integrates ships + fires (spawns a `Projectile` at the nose when `Firing` and the per-class cooldown elapsed, tracked via new `Ship.LastFireTick` vs `Match.Tick`); Pass B advances projectiles by `dt`, culls at `ExpiresAtTick`, resolves hits (blocked by asteroids; damages enemy ships — friendly fire skipped by `Team`); Pass C does collisions then applies accumulated damage and kills at `Health<=0` (`KillShip` deletes the row + input buffer + clears `Player.ShipId`). New fields: `Ship.LastFireTick`, `Projectile.Damage`. **Combat tuning** (`.PLAN/03`): Scout dmg 4 / interval 4 ticks, Fighter dmg 10 / interval 8; muzzle 250 u/s (+ship vel), 2.5 s lifespan, ship/proj hit radii 3/1. **Spawn now faces sector center** (yaw so local +Z points base→origin) — a gameplay improvement that also makes the head-on combat test deterministic. **Client:** `ProjectileView` renders each shot by velocity-extrapolation from the last sample; `WorldRenderer` does projectile insert/update/delete (bright unshaded sphere); HUD shows `HP cur/max`; `Firing` wired to **Space / left-mouse**; `--combat-test` dev flag flies straight + fires. **Verified headless:** projectiles spawn/advance/cull in a bounded rolling window; two opposing `--combat-test` clients went head-on and **both ships were destroyed, `Player.ShipId` cleared on each** (respawn menu reappears), match stayed `Active`; lone firer took no self damage. Subjective combat feel needs a human. | Yes (playtest) |
| 2026-06-01 | Collision scope (T8) | Implemented ship-vs-asteroid, ship-vs-**enemy**-base (own base is the dock/spawn → pass-through, avoids spawning-inside-base self-damage), and enemy ship-vs-ship (pairwise mutual damage + separation). All use an inward-impact model: damage ∝ inward speed (capped 30/tick), damped bounce (restitution 0.3), push to surface. Resting contact (no inward velocity) deals no damage. Projectile→**base** damage + win condition is deferred to **T9** (projectiles currently fly through bases). | No |
| 2026-06-01 | Godot headless arg passing (T7) | `OS.GetCmdlineArgs()` returns engine args only — args after a `--` separator go to `OS.GetCmdlineUserArgs()` instead. So `--autofly`/`--fighter` must be passed **before** any `--` (e.g. `Godot --headless --path client --autofly --fighter`), not after it, or they're invisible to `ShipController`. (Cost ~20 min when a `--`-separated launch silently never spawned.) | No |
