[project]
name = "asteroid-gen"
version = "0.1.0"
description = "Deterministic seeded asteroid mesh + normal-map generator (OpenSCAD + Python)"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "pillow>=10.0",
    "pygltflib>=1.16",
]

[tool.uv]
package = false
