// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#nullable enable

using System;
using SpacetimeDB.ClientApi;
using System.Collections.Generic;
using System.Runtime.Serialization;

namespace SpacetimeDB.Types
{
    public sealed partial class RemoteReducers : RemoteBase
    {
        public delegate void SpawnShipHandler(ReducerEventContext ctx, SpacetimeDB.Types.ShipClass shipClass);
        public event SpawnShipHandler? OnSpawnShip;

        public void SpawnShip(SpacetimeDB.Types.ShipClass shipClass)
        {
            conn.InternalCallReducer(new Reducer.SpawnShip(shipClass));
        }

        public bool InvokeSpawnShip(ReducerEventContext ctx, Reducer.SpawnShip args)
        {
            if (OnSpawnShip == null)
            {
                if (InternalOnUnhandledReducerError != null)
                {
                    switch (ctx.Event.Status)
                    {
                        case Status.Failed(var reason): InternalOnUnhandledReducerError(ctx, new Exception(reason)); break;
                        case Status.OutOfEnergy(var _): InternalOnUnhandledReducerError(ctx, new Exception("out of energy")); break;
                    }
                }
                return false;
            }
            OnSpawnShip(
                ctx,
                args.ShipClass
            );
            return true;
        }
    }

    public abstract partial class Reducer
    {
        [SpacetimeDB.Type]
        [DataContract]
        public sealed partial class SpawnShip : Reducer, IReducerArgs
        {
            [DataMember(Name = "ship_class")]
            public ShipClass ShipClass;

            public SpawnShip(ShipClass ShipClass)
            {
                this.ShipClass = ShipClass;
            }

            public SpawnShip()
            {
            }

            string IReducerArgs.ReducerName => "spawn_ship";
        }
    }
}
